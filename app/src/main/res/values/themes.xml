<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FirebaseChat" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/colorRed</item>
        <item name="colorPrimaryVariant">@color/colorRed</item>
        <item name="colorSecondary">@color/colorRed</item>
        <item name="colorOnPrimary">#ffffff</item>
        <item name="colorOnSecondary">@color/colorRed</item>
    </style>

    <style name="SplashTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="MyNewSplashTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#ff0000</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_user</item>
        <item name="windowSplashScreenAnimationDuration">2000</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.FirebaseChat</item>
    </style>


</resources>